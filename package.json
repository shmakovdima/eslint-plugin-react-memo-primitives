{
  "name": "react-memo-primitives-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.65.0"
  }
}
