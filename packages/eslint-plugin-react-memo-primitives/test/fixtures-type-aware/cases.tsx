// Placeholder file so `tsconfig.json`'s `include` matches at least one real file on disk — the
// actual test cases are injected into the checker via RuleTester's `code` + `filename` pointing
// at this path (see require-memo-primitives-type-aware.test.js), not by editing this file itself.
export {};
